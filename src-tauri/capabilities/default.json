{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the AgentDeck main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "autostart:default"
  ]
}
