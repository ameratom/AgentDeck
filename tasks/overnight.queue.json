[
  {
    "id": "task-015",
    "title": "Compact Providers view header and list shell to match Settings unified-row pattern",
    "scope": "ALLOW",
    "files_hint": [
      "src/features/providers/ProvidersView.tsx",
      "src/features/providers/components/ProviderList.tsx",
      "src/styles/globals.css"
    ],
    "acceptance": [
      "pnpm verify passes",
      "Providers uses compact workspace classes consistent with Projects and Audit",
      "Provider list and detail panes remain usable without widening the layout"
    ],
    "deferred_if": "requires provider schema or Tauri API changes"
  },
  {
    "id": "task-016",
    "title": "Log patch apply failures in Hermes morning report with sanitized diff preview",
    "scope": "ALLOW",
    "files_hint": [
      "src-tauri/src/autonomy/overnight.rs",
      "src-tauri/src/autonomy/composer_bridge.rs"
    ],
    "acceptance": [
      "pnpm verify passes",
      "Blocked tasks include first hunk preview when git apply fails",
      "sanitize_patch_for_apply has unit coverage for bare context lines"
    ],
    "deferred_if": "requires changes to morning report file format"
  },
  {
    "id": "task-017",
    "title": "Probe cloudflared connector status in ChatGPT review health when public MCP uses a custom hostname",
    "scope": "ALLOW",
    "files_hint": [
      "scripts/check-chatgpt-review-health.sh",
      "src-tauri/src/chatgpt_review.rs"
    ],
    "acceptance": [
      "pnpm verify passes",
      "Health check warns when public tools/list fails with HTTP 530 and cloudflared has no connector",
      "Hermes review detail references install-cloudflared-service.sh recovery path"
    ],
    "deferred_if": "requires cloudflared credentials in CI"
  }
]